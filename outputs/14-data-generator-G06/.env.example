# Copy to .env and replace the password locally. The load command reads it.
# Never commit .env. Exported Bash variables override these file values.
DB_SERVER=localhost,1433
DB_DATABASE=School
DB_USERNAME=sa
DB_PASSWORD=replace_with_existing_private_password
