# CLAUDE.md — cs486-demo

Claude Code: read `AGENTS.md` and `MEMORY.md` in this same directory first
— they contain the full project rules and current state. This file exists
only because Claude Code looks for `CLAUDE.md` specifically and does not
read `AGENTS.md` on its own.

Everything in AGENTS.md applies as-is. The skill is at
`.claude/skills/db-design-pipeline/SKILL.md` (a copy of the canonical
version in `.opencode/skills/db-design-pipeline/SKILL.md` — keep them in
sync if you edit either one).

Do not duplicate rules here. If AGENTS.md and this file ever conflict,
AGENTS.md wins — fix this file instead of treating it as an override.
