# LaTeX build artifacts
report/.aux
report/*.synctex.gz
report/*.pdf
report/*.aux
report/*.fdb_latexmk
.aux
*.synctex.gz
*.pdf
report/*.aux
report/*.fdb_latexmk

assets/*.bkp

# macOS junk
.DS_Store
__MACOSX/

# Agent / IDE state (don't commit per-session state)
.opencode/state/
.claude/state/

# API keys
.env
.env.local